export enum Room {
  master = 'master',
  living = 'living',
  kitchen = 'kitchen',
  guest = 'guest',
}

export enum Active {
  on = 'on',
  off = 'off',
}

export enum FanSpeed {
  off = 'off',
  low = 'low',
  mid = 'mid',
  high = 'high',
  auto = 'auto',
}
